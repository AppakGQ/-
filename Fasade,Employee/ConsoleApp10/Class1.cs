using Microsoft.VisualBasic;
using MyNamespace;

DateTime dateTime = new DateTime(2024, 12, 18);
DateTime dateTimeend = new DateTime(2024, 12, 15);

HotelFacade.BookRoom(dateTime, dateTimeend, 2, "") ;